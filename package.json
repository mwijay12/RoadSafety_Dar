{
  "name": "roadsafety-dar",
  "private": true,
  "scripts": {
    "build": "cd landing && npx prisma generate && npx next build",
    "dev": "cd landing && npm run dev",
    "start": "cd landing && npx next start"
  },
  "engines": { "node": ">=20" }
}
